plugins {
    id 'org.owasp.dependencycheck' version '5.0.0-M3.1'
}

allprojects {
    repositories {
        jcenter()
    }
}

subprojects {
    version = '0.1'

    dependencyCheck {
        scanSet = [
                rootProject.relativePath(project.buildDir)
        ]
    }
}
